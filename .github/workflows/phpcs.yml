name: "PHP Code Sniffer"

on:
  pull_request:

jobs:
  phpcs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0 # important!
          
      - name: Check
        run: |
          php --version
          ls -al
          pwd

